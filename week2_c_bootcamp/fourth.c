#include<stdio.h>

int main () {
    int width;
    int height;
    printf("insert a width: ");
    scanf("%d",);
    printf("insert a height: ");
    scanf("%d", &height);
    int area = width * height;
    printf("area = %d\n", area);
    return 0;
}